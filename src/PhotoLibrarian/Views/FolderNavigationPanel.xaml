<UserControl
    x:Class="PhotoLibrarian.Views.FolderNavigationPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PhotoLibrarian.ViewModels">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header with actions -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Library" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="4">
                <Button x:Name="AddFolderButton" ToolTipService.ToolTip="Add folder"
                        Click="OnAddFolderClick" Padding="6">
                    <FontIcon Glyph="&#xE710;" FontSize="14" />
                </Button>
                <Button x:Name="RefreshButton" ToolTipService.ToolTip="Refresh"
                        Click="OnRefreshClick" Padding="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Show All button -->
        <Button Grid.Row="1" Content="Show All Photos" HorizontalAlignment="Stretch"
                Margin="0,0,0,8" Click="OnShowAllClick" />

        <!-- Folder Tree -->
        <TreeView Grid.Row="2" x:Name="FolderTree"
                  ItemInvoked="OnFolderInvoked">
        </TreeView>
    </Grid>
</UserControl>
